# IMMOHRTAL Blender workspace

The reusable Pittsburgh concert venue source belongs here.

Expected structure:

```text
blender/
  immohrtal-pittsburgh-concert.blend
  scripts/
    build_scene.py
  previews/
  references/
```

Commit the reproducible script and compact review previews. Keep caches and high-resolution render sequences out of Git. Export only optimized website assets to `public/models/`.
